我将修改 `c:\AI\AI_TSC\src\script_analyzer.py` 以实现提示词模板保存、深色主题、文案修正及交互优化。

**计划详情：**

1.  **提示词模板保存功能**:
    *   在“提示词配置”对话框中增加“保存为模板”和“加载模板”的功能。
    *   使用本地 JSON 文件 (`prompt_templates.json`) 来持久化存储用户的自定义提示词模板。
    *   用户可以给模板命名并保存，下次直接选择加载。

2.  **文案与逻辑修正**:
    *   **界面文案**：统一将“脚本”改为“剧本”（如“剧本内容”、“上传剧本”、“分析剧本”）。
    *   **提示词逻辑**：默认提示词不再强制包含 `{script}`。构建请求时，如果提示词中没有占位符，系统会自动将剧本内容拼接在提示词之后。

3.  **深色主题 (Dark Mode)**:
    *   实现全局深色样式（背景 `#2b2b2b`，文字浅色）。
    *   适配所有控件（Text, Entry, Button, Frame, Scrollbar 等）。

4.  **默认配置更新**:
    *   默认 API URL 设为 `https://ai.t8star.cn`。

5.  **布局与滚动优化**:
    *   增加底部内边距，解决显示不全问题。
    *   实现智能滚轮（文本框边界自动滚动页面）。

**修改文件**: `c:\AI\AI_TSC\src\script_analyzer.py`
