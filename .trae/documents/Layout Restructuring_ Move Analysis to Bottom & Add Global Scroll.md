根据您的最新要求，我更新了实施计划：

**计划详情：**

1.  **全局滚动条与鼠标滚轮**:
    -   在主界面创建一个全局的可滚动区域（Canvas + Scrollbar），包含所有的内容。
    -   添加鼠标滚轮事件监听，支持滚轮上下滑动。

2.  **布局结构调整（支持高度调节）**:
    -   使用 **垂直分割窗口 (Vertical PanedWindow)** 作为主容器。这将允许您通过拖动中间的分隔条来**自由调节**上方（脚本/结果）和下方（拆分结果）区域的高度。
    -   **上半部分（第一行）**：使用 **水平分割窗口 (Horizontal PanedWindow)**。左侧放置“脚本内容”，右侧放置“分析结果”。这样左右宽度也可以调节。
    -   **下半部分（第二行）**：放置“结果分析拆分”区域。
        -   内部布局改为 **网格排列**：每行 2 个框，共 4 行。

3.  **保存功能增强**:
    -   **第一行分析结果**：保留并确保右侧“分析结果”区域的“保存结果”按钮可用。
    -   **第二行拆分结果**：为下方的 8 个拆分结果框**逐一添加独立的“保存”按钮**。
    -   实现相应的保存逻辑，点击按钮即可保存对应框内的文本内容。

**修改文件**: `c:\AI\AI_TSC\src\script_analyzer.py`
